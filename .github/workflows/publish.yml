on:
  push:
    branches:
    - master
  release:
    types: [ prereleased, released]

jobs:
  downloadBinary:
    uses: simanasch/vscodetalker/.github/workflows/install-tts-service.yml@downloadBinary
  publish:
    name: 拡張機能を公開_vsCode
    runs-on: windows-latest
    # if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Nodeをインストール
        uses: actions/setup-node@v3
      - name: node_modulesのインストール
        run: npm install
      - name: 拡張機能を公開
        run: |
          npm run deploy
        env:
          VSCE_PAT: ${{secrets.VSCE_PAT}}